# GitLab API 访问令牌（必填）
# 在 GitLab: Settings -> Access Tokens 创建，需 read_api 权限
GITLAB_PRIVATE_TOKEN=your_gitlab_private_token_here

# GitLab 实例地址（可选，默认为 https://gitlab.com）
# 自建 GitLab 请改为你的实例地址，例如 https://gitlab.example.com
GITLAB_BASE_URL=https://gitlab.com

# 仓库配置文件路径（可选，默认为项目根目录下的 repos_config.json）
# GIT_AI_REPOS_CONFIG=/path/to/repos_config.json

# 服务端口与主机（可选）
# PORT=5000
# HOST=0.0.0.0
